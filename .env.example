# Optional Upstash Redis configuration for project view counts.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional GitHub token for README synchronization during local scripts or CI.
GITHUB_TOKEN=
