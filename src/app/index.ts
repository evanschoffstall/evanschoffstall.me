/** Public surface for the top-level App Router segment. */
export { default as GlobalError } from "./error";
export { metadata, default as RootLayout } from "./layout";
export { default as HomePage, revalidate } from "./page";